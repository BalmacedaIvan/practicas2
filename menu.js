function rutina(event) {
    event.preventDefault(); // Evita el comportamiento por defecto del enlace
    window.location.href = 'miRutina.html'; // Redirige a la página de registro
}
function actividades(event) {
    event.preventDefault(); // Evita el comportamiento por defecto del enlace
    window.location.href = 'actividades.html'; // Redirige a la página de registro
}